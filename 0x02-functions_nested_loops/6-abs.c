#include "main.h"

/**
 * _abs - Print absolute value.
 * @n: n is an integer,
 * Return: absolute value of integer.
 */

int _abs(int n)
{
if (n > 0)
{
return (n);
}
else if (n < 0)
{
return (-n);
}
else
{
return (n);
}
}
